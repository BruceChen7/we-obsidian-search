import { Vault } from 'obsidian';
import { WeSearchSettings } from './setting';
export async function connectSemantra(vault: Vault, setting: WeSearchSettings, notify: boolean = true) {
	return true;
}

export async function connectKhoj(vault: Vault, setting: WeSearchSettings, notify: boolean = true) {
	return true;

}

export async function connectRg(vault: Vault, setting: WeSearchSettings, notify: boolean = true) {
	return true;

}
